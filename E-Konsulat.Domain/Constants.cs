namespace E_Konsulat.Domain
{
    public enum SelectorType
    {
        Id,
        Class
    }

    public enum NodeTypes
    {
        Aggregation,
        Select,
        MultiSelect,
        Text
    }
}
